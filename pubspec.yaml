name: ayana_fixed

description: "Ho'oponopono & Ayana AI Spiritual Guide App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Network
  http: ^1.2.1

  # UI
  lucide_icons: ^0.257.0
  flutter_markdown: ^0.6.21
  cupertino_icons: ^1.0.8

  # AI (Gemini)
  google_generative_ai: ^0.3.0

  # Device
  image_picker: ^1.0.7
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
